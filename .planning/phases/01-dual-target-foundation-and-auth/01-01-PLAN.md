---
phase: 01-dual-target-foundation-and-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fde_sql_mcp/config.py
  - src/fde_sql_mcp/server.py
  - src/fde_sql_mcp/tools/auth.py
  - fde_sql_mcp.config.template.json
  - README.md
  - tests/test_settings_fabric.py
autonomous: true
requirements:
  - ROUTE-01
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - CONF-01
user_setup: []
must_haves:
  truths:
    - "Server startup remains valid for on-prem-only configuration paths."
    - "Fabric auth mode resolves to client_secret only when all secret fields are configured."
    - "Fabric auth mode resolves to fallback mode when client-secret fields are incomplete."
    - "Fabric auth configuration is single-tenant and reports one resolved tenant context."
  artifacts:
    - path: "src/fde_sql_mcp/config.py"
      provides: "Dual-target settings model and deterministic Fabric auth mode resolution inputs."
      contains: "fabric_auth_mode"
    - path: "src/fde_sql_mcp/tools/auth.py"
      provides: "Non-secret auth diagnostics payload."
      contains: "get_auth_info_impl"
    - path: "tests/test_settings_fabric.py"
      provides: "Regression and precedence coverage for Phase 1 requirements."
      min_lines: 40
  key_links:
    - from: "src/fde_sql_mcp/server.py"
      to: "src/fde_sql_mcp/tools/auth.py"
      via: "MCP tool delegation"
      pattern: "get_auth_info_impl"
---

<objective>
Implement Phase 1 dual-target and auth foundation for the MCP server.

Purpose: establish deterministic Fabric authentication behavior and unified config without changing on-prem tool usage.
Output: additive Fabric config/auth code paths, auth diagnostics tool, tests, and updated setup docs/template.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-dual-target-foundation-and-auth/01-CONTEXT.md
@.planning/phases/01-dual-target-foundation-and-auth/01-RESEARCH.md
@src/fde_sql_mcp/config.py
@src/fde_sql_mcp/server.py
@README.md
</context>

<threat_model>
## Threat Model

| threat_id | category | files | disposition | mitigation |
|-----------|----------|-------|-------------|------------|
| T-01 | Config regression breaks on-prem behavior | `src/fde_sql_mcp/config.py` | mitigate | Preserve current SQL defaults/env names and add regression tests for existing behavior. |
| T-02 | Auth mode ambiguity causes wrong credential path | `src/fde_sql_mcp/config.py`, `tests/test_settings_fabric.py` | mitigate | Implement explicit precedence function requiring all 3 client-secret vars, with tests for partial/complete cases. |
| T-03 | Sensitive secret exposure in diagnostics | `src/fde_sql_mcp/tools/auth.py`, `src/fde_sql_mcp/server.py` | mitigate | Return only mode/source/tenant metadata; never return raw secret values. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend runtime settings for dual-target foundation</name>
  <files>src/fde_sql_mcp/config.py</files>
  <read_first>src/fde_sql_mcp/config.py, fde_sql_mcp.config.template.json, .planning/phases/01-dual-target-foundation-and-auth/01-CONTEXT.md</read_first>
  <action>Add Fabric settings fields to `Settings` in `config.py` using existing helper style: `fabric_enabled`, `fabric_tenant_id`, `fabric_client_id`, `fabric_client_secret`, `fabric_auth_fallback_mode`, `fabric_default_workspace`, `fabric_default_database`, `fabric_allowed_workspaces`, and `fabric_allowed_databases`. Add deterministic derived fields or helper properties: `fabric_auth_mode` (`client_secret` only when tenant/client/secret all present; otherwise fallback mode) and `fabric_tenant_context` (single resolved tenant value or `None`). Keep existing SQL settings defaults and environment variable names unchanged.</action>
  <verify>.venv\Scripts\python -m pytest tests/test_settings_fabric.py -q -k "existing_sql_defaults or fabric_auth_mode"</verify>
  <acceptance_criteria>
    - `src/fde_sql_mcp/config.py` contains `fabric_auth_mode` with explicit all-three-secret check.
    - `src/fde_sql_mcp/config.py` still defines existing SQL fields (`sql_server`, `sql_database`, `sql_max_rows`) with unchanged default behavior.
    - `src/fde_sql_mcp/config.py` defines single-tenant context field/property (`fabric_tenant_context`).
  </acceptance_criteria>
  <done>Dual-target settings are additive and deterministic without breaking current on-prem defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Add auth diagnostics implementation and MCP tool wiring</name>
  <files>src/fde_sql_mcp/tools/auth.py, src/fde_sql_mcp/server.py</files>
  <read_first>src/fde_sql_mcp/server.py, src/fde_sql_mcp/tools/databases.py, src/fde_sql_mcp/config.py</read_first>
  <action>Create `src/fde_sql_mcp/tools/auth.py` with `get_auth_info_impl()` returning a non-secret dictionary containing: `fabric_enabled`, `auth_mode`, `tenant_context`, `credential_source`, and booleans for whether each credential input is configured (not values). Add MCP tool `get_auth_info` in `server.py` delegating to the new impl via `asyncio.to_thread`. Keep existing SQL tool signatures unchanged.</action>
  <verify>.venv\Scripts\python -m pytest tests/test_settings_fabric.py -q -k "auth_diagnostics"</verify>
  <acceptance_criteria>
    - `src/fde_sql_mcp/server.py` contains `async def get_auth_info()` with a tool description.
    - `src/fde_sql_mcp/tools/auth.py` contains `def get_auth_info_impl()` and does not include any raw secret value in returned payload.
    - Existing tools like `list_databases` and `run_readonly_query` remain present in `server.py`.
  </acceptance_criteria>
  <done>Auth diagnostics are exposed safely through MCP without changing existing SQL tool contract.</done>
</task>

<task type="auto">
  <name>Task 3: Document and test Phase 1 behavior contract</name>
  <files>tests/test_settings_fabric.py, fde_sql_mcp.config.template.json, README.md</files>
  <read_first>README.md, fde_sql_mcp.config.template.json, src/fde_sql_mcp/config.py</read_first>
  <action>Add `tests/test_settings_fabric.py` covering: SQL default compatibility, client-secret precedence, fallback mode when partial credentials, and diagnostics payload secrecy. Update config template with Fabric keys and safe placeholders. Update README configuration section with Fabric env variables, precedence rules, and `get_auth_info` tool usage.</action>
  <verify>.venv\Scripts\python -m pytest -q</verify>
  <acceptance_criteria>
    - `tests/test_settings_fabric.py` includes tests named for client-secret precedence and fallback behavior.
    - `fde_sql_mcp.config.template.json` includes Fabric keys (`fabric_tenant_id`, `fabric_client_id`, `fabric_client_secret`).
    - `README.md` documents `FABRIC_TENANT_ID`, `FABRIC_CLIENT_ID`, `FABRIC_CLIENT_SECRET`, and fallback behavior when incomplete.
  </acceptance_criteria>
  <done>Phase 1 behavior is codified by tests and docs for deterministic future phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv\Scripts\python -m pytest -q` passes.
- [ ] `src/fde_sql_mcp/server.py` includes new `get_auth_info` tool and existing SQL tools remain unchanged.
- [ ] README and config template include dual-target Fabric configuration details.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No regressions to existing on-prem SQL workflow
- Requirement IDs `ROUTE-01`, `AUTH-01`, `AUTH-02`, `AUTH-03`, `CONF-01` are satisfied by implemented behavior and tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-dual-target-foundation-and-auth/01-01-SUMMARY.md`
</output>

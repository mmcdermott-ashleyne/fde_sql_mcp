---
phase: 02-target-routing-and-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fde_sql_mcp/config.py
  - src/fde_sql_mcp/server.py
  - src/fde_sql_mcp/tools/databases.py
  - src/fde_sql_mcp/tools/targeting.py
  - fde_sql_mcp.config.template.json
  - README.md
  - tests/test_target_routing.py
autonomous: true
requirements:
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
  - FAB-01
  - SAFE-03
  - CONF-02
user_setup: []
must_haves:
  truths:
    - "User can explicitly switch active target between onprem and fabric."
    - "User can provide natural-language fabric target hints and get deterministic target resolution."
    - "Fabric workspace/database selections are rejected when outside configured allowlists."
    - "Read-only query responses include resolved target context."
    - "Existing SQL tool names/signatures remain unchanged while routing tools are additive."
  artifacts:
    - path: "src/fde_sql_mcp/tools/targeting.py"
      provides: "Target resolution, active target state, and allowlist governance checks."
      contains: "set_query_target_impl"
    - path: "src/fde_sql_mcp/server.py"
      provides: "Additive target-management MCP tools while keeping SQL tools intact."
      contains: "set_query_target"
    - path: "src/fde_sql_mcp/tools/databases.py"
      provides: "Target-aware query response context for read-only query output."
      contains: "target_context"
    - path: "tests/test_target_routing.py"
      provides: "Deterministic routing/governance regression coverage."
      min_lines: 80
  key_links:
    - from: "src/fde_sql_mcp/server.py"
      to: "src/fde_sql_mcp/tools/targeting.py"
      via: "MCP tool delegation"
      pattern: "set_query_target_impl"
    - from: "src/fde_sql_mcp/tools/databases.py"
      to: "src/fde_sql_mcp/tools/targeting.py"
      via: "resolved target context"
      pattern: "get_active_target_impl"
---

<objective>
Implement Phase 2 target routing and governance.

Purpose: provide deterministic environment routing controls and Fabric allowlist enforcement while preserving the existing SQL MCP tool contract.
Output: target routing tools, parser/governance logic, query response target context, and tests/docs updates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-target-routing-and-governance/02-CONTEXT.md
@.planning/phases/02-target-routing-and-governance/02-RESEARCH.md
@src/fde_sql_mcp/config.py
@src/fde_sql_mcp/server.py
@src/fde_sql_mcp/tools/databases.py
</context>

<threat_model>
## Threat Model

| threat_id | category | files | disposition | mitigation |
|-----------|----------|-------|-------------|------------|
| T-01 | Ambiguous natural-language input resolves to wrong target | `src/fde_sql_mcp/tools/targeting.py`, `tests/test_target_routing.py` | mitigate | Deterministic token matching with explicit ambiguity errors and tests for conflicting/unknown hints. |
| T-02 | Allowlist bypass through workspace IDs or ad-hoc names | `src/fde_sql_mcp/config.py`, `src/fde_sql_mcp/tools/targeting.py`, `tests/test_target_routing.py` | mitigate | Add explicit workspace ID mapping config and canonical allowlist re-check after mapping. |
| T-03 | Query response context does not match actual execution target | `src/fde_sql_mcp/tools/databases.py`, `src/fde_sql_mcp/tools/targeting.py`, `tests/test_target_routing.py` | mitigate | Build `target_context` from the resolved active target used for execution path selection. |
| T-04 | Tool contract churn breaks existing prompt usage | `src/fde_sql_mcp/server.py`, `README.md` | mitigate | Keep current SQL tool names/signatures unchanged; add routing tools separately and document additive behavior. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic target router and MCP tool wiring</name>
  <files>src/fde_sql_mcp/config.py, src/fde_sql_mcp/tools/targeting.py, src/fde_sql_mcp/server.py</files>
  <read_first>src/fde_sql_mcp/config.py, src/fde_sql_mcp/server.py, .planning/phases/02-target-routing-and-governance/02-CONTEXT.md</read_first>
  <action>Add routing config support in `config.py` for workspace ID mapping (`fabric_workspace_id_map`) and helper normalization. Create `src/fde_sql_mcp/tools/targeting.py` with an in-process active target store defaulting to `onprem`, deterministic target resolution (`onprem`/`fabric`), natural-language hint parsing for `dev/stg/prod`, `warehouse/lakehouse`, and allowlisted database names, plus strict allowlist enforcement. Add MCP wrappers in `server.py`: `set_query_target(target: str)`, `get_query_target()`, and `list_fabric_workspaces()` delegating to targeting impl functions.</action>
  <verify>.venv\Scripts\python -m pytest tests/test_target_routing.py -q -k "set_query_target or list_fabric_workspaces or resolve"</verify>
  <acceptance_criteria>
    - `src/fde_sql_mcp/tools/targeting.py` contains `set_query_target_impl`, `get_query_target_impl`, and `list_fabric_workspaces_impl`.
    - `src/fde_sql_mcp/config.py` includes `fabric_workspace_id_map` parsing with deterministic string normalization.
    - `src/fde_sql_mcp/server.py` contains additive tools `set_query_target`, `get_query_target`, and `list_fabric_workspaces` without removing existing SQL tools.
  </acceptance_criteria>
  <done>Active target routing and governance entrypoints are available through MCP.</done>
</task>

<task type="auto">
  <name>Task 2: Add target context to query results with governance-aware routing</name>
  <files>src/fde_sql_mcp/tools/databases.py, src/fde_sql_mcp/tools/targeting.py</files>
  <read_first>src/fde_sql_mcp/tools/databases.py, src/fde_sql_mcp/tools/targeting.py, .planning/REQUIREMENTS.md</read_first>
  <action>Update `run_readonly_query_impl` in `databases.py` to retrieve resolved active target context from targeting helpers and include it in the returned payload under `target_context`. Preserve existing `database`, `query`, `max_rows` signature and current read-only validation behavior. If active environment is `fabric`, enforce Phase 2 governance by validating selected workspace/database against allowlists and return a clear unsupported execution error until Phase 3 endpoint execution is implemented.</action>
  <verify>.venv\Scripts\python -m pytest tests/test_target_routing.py -q -k "target_context or fabric_execution_not_supported"</verify>
  <acceptance_criteria>
    - `src/fde_sql_mcp/tools/databases.py` returns `target_context` in `run_readonly_query_impl` payload.
    - Existing keys `rows`, `row_count`, `row_limit`, and `truncated` remain in query response.
    - Fabric-selected target path rejects execution with explicit message indicating endpoint execution arrives in Phase 3.
  </acceptance_criteria>
  <done>Query responses expose deterministic target context without breaking current SQL tool signature.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests and update operator docs/template</name>
  <files>tests/test_target_routing.py, README.md, fde_sql_mcp.config.template.json</files>
  <read_first>README.md, fde_sql_mcp.config.template.json, src/fde_sql_mcp/tools/targeting.py, src/fde_sql_mcp/server.py</read_first>
  <action>Create `tests/test_target_routing.py` covering explicit target switches, deterministic natural-language resolution, allowlist rejection, workspace ID mapping, and query `target_context` output behavior. Update `fde_sql_mcp.config.template.json` with `fabric_workspace_id_map` examples. Update README tools/config sections to document new routing tools and additive contract behavior while noting Fabric SQL execution parity is Phase 3.</action>
  <verify>.venv\Scripts\python -m pytest -q</verify>
  <acceptance_criteria>
    - `tests/test_target_routing.py` includes tests for explicit switch, natural-language resolution, and allowlist rejection paths.
    - `README.md` documents `set_query_target`, `get_query_target`, and `list_fabric_workspaces` with governance behavior.
    - `fde_sql_mcp.config.template.json` includes `fabric_workspace_id_map`.
  </acceptance_criteria>
  <done>Phase 2 behavior is codified and documented with regression protection.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv\Scripts\python -m pytest -q` passes.
- [ ] `run_readonly_query` response includes `target_context` and existing payload keys.
- [ ] New routing tools are present and existing SQL tool signatures remain intact.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Requirement IDs `ROUTE-02`, `ROUTE-03`, `ROUTE-04`, `FAB-01`, `SAFE-03`, `CONF-02` are satisfied by implemented behavior/tests/docs
</success_criteria>

<output>
After completion, create `.planning/phases/02-target-routing-and-governance/02-01-SUMMARY.md`
</output>

---
phase: 03-fabric-sql-endpoint-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fde_sql_mcp/config.py
  - src/fde_sql_mcp/clients/sql.py
  - src/fde_sql_mcp/tools/databases.py
  - README.md
  - fde_sql_mcp.config.template.json
  - tests/test_target_routing.py
  - tests/test_settings_fabric.py
autonomous: true
requirements:
  - FAB-02
  - FAB-03
  - FAB-04
  - FAB-05
  - SAFE-04
user_setup: []
must_haves:
  truths:
    - "User can set/query Fabric Warehouse targets only for allowed core_dw contexts."
    - "User can set/query Fabric Lakehouse targets only for allowed core_lh contexts."
    - "run_readonly_query executes against selected Fabric SQL endpoint via the same MCP SQL contract."
    - "Metadata discovery tools execute with target-aware behavior for onprem/fabric where endpoint SQL supports them."
    - "No non-SQL Fabric operations are exposed by this MCP server."
  artifacts:
    - path: "src/fde_sql_mcp/tools/databases.py"
      provides: "Target-aware query/metadata execution path for onprem and fabric."
      contains: "_resolve_execution_target"
    - path: "src/fde_sql_mcp/config.py"
      provides: "Fabric SQL endpoint mapping configuration model."
      contains: "fabric_sql_endpoint_map"
    - path: "tests/test_target_routing.py"
      provides: "Phase 3 regression coverage for Fabric warehouse/lakehouse parity and metadata routing."
      min_lines: 120
  key_links:
    - from: "src/fde_sql_mcp/tools/databases.py"
      to: "src/fde_sql_mcp/tools/targeting.py"
      via: "active target resolution"
      pattern: "get_query_target_impl"
    - from: "src/fde_sql_mcp/tools/databases.py"
      to: "src/fde_sql_mcp/clients/sql.py"
      via: "target-specific SQL connection creation"
      pattern: "get_sql_connection"
---

<objective>
Implement Phase 3 Fabric SQL endpoint parity.

Purpose: execute read-only SQL and metadata operations against allowlisted Fabric Warehouse/Lakehouse SQL endpoints through the existing MCP SQL contract.
Output: target-aware execution layer, Fabric endpoint mapping config/docs, and regression coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-fabric-sql-endpoint-parity/03-CONTEXT.md
@.planning/phases/03-fabric-sql-endpoint-parity/03-RESEARCH.md
@src/fde_sql_mcp/config.py
@src/fde_sql_mcp/clients/sql.py
@src/fde_sql_mcp/tools/databases.py
@src/fde_sql_mcp/tools/targeting.py
</context>

<threat_model>
## Threat Model

| threat_id | category | files | disposition | mitigation |
|-----------|----------|-------|-------------|------------|
| T-01 | Incorrect Fabric endpoint selection enables wrong-environment access | `src/fde_sql_mcp/config.py`, `src/fde_sql_mcp/tools/databases.py`, `tests/test_target_routing.py` | mitigate | Require explicit workspace+endpoint mapping and fail closed if mapping missing/mismatched. |
| T-02 | Query context and execution target diverge | `src/fde_sql_mcp/tools/databases.py`, `tests/test_target_routing.py` | mitigate | Build connection params from active target used to produce `target_context`. |
| T-03 | Non-SQL Fabric functionality accidentally exposed | `src/fde_sql_mcp/server.py`, `README.md` | mitigate | Do not add non-SQL MCP tools; document SQL-only scope in phase-3 behavior. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Fabric SQL endpoint mapping settings and parsing</name>
  <files>src/fde_sql_mcp/config.py, fde_sql_mcp.config.template.json, README.md, tests/test_settings_fabric.py</files>
  <read_first>src/fde_sql_mcp/config.py, fde_sql_mcp.config.template.json, README.md</read_first>
  <action>Add `fabric_sql_endpoint_map` setting that supports local JSON and env var parsing into a deterministic nested mapping `{workspace: {endpoint_type: {server, database, user, password}}}`. Document it in template and README, with env var support (`FABRIC_SQL_ENDPOINT_MAP`). Add config parsing tests.</action>
  <verify>.venv\Scripts\python -m pytest tests/test_settings_fabric.py -q</verify>
  <acceptance_criteria>
    - `Settings` includes `fabric_sql_endpoint_map` with normalized, non-empty nested values only.
    - Template and README describe warehouse/lakehouse endpoint map for allowlisted workspaces.
    - Parsing tests cover valid JSON and invalid/partial input handling.
  </acceptance_criteria>
  <done>Fabric endpoint connection metadata is configurable and validated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement target-aware onprem/fabric SQL execution core</name>
  <files>src/fde_sql_mcp/clients/sql.py, src/fde_sql_mcp/tools/databases.py, tests/test_target_routing.py</files>
  <read_first>src/fde_sql_mcp/tools/databases.py, src/fde_sql_mcp/tools/targeting.py, src/fde_sql_mcp/clients/sql.py</read_first>
  <action>Replace the Phase 2 Fabric execution gate with a target-aware connection resolver. For on-prem, preserve current behavior; for Fabric, resolve connection params from `fabric_sql_endpoint_map` + active target context and enforce endpoint/database compatibility (`warehouse/core_dw`, `lakehouse/core_lh`) before connect. Reuse same path for `run_readonly_query_impl` and metadata `_fetch_rows` helpers so behavior is parity-based where supported.</action>
  <verify>.venv\Scripts\python -m pytest tests/test_target_routing.py -q -k "fabric or metadata or target_context"</verify>
  <acceptance_criteria>
    - `run_readonly_query_impl` executes on Fabric when target is Fabric and endpoint map exists.
    - Metadata helpers use the same target-aware resolver.
    - Missing/mismatched Fabric endpoint configuration returns clear validation errors.
  </acceptance_criteria>
  <done>Shared SQL execution path supports onprem and allowlisted Fabric endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Add parity regression coverage and finalize docs</name>
  <files>tests/test_target_routing.py, README.md</files>
  <read_first>tests/test_target_routing.py, README.md, src/fde_sql_mcp/tools/databases.py</read_first>
  <action>Add regression tests for warehouse/lakehouse Fabric query execution, target context consistency, metadata routing over Fabric where supported, and fail-closed behavior for unmapped targets. Update README to state phase-3 parity behavior and SQL-only boundary.</action>
  <verify>.venv\Scripts\python -m pytest -q</verify>
  <acceptance_criteria>
    - Tests prove FAB-02/03/04/05 behavior and fail-closed cases.
    - README removes the Phase-2 unsupported statement and documents phase-3 Fabric parity.
    - No new non-SQL MCP tools introduced.
  </acceptance_criteria>
  <done>Phase 3 behavior is codified, documented, and regression-protected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv\Scripts\python -m pytest -q` passes.
- [ ] `run_readonly_query` includes stable payload keys plus `target_context` for onprem/fabric.
- [ ] Metadata discovery helpers route through target-aware execution path.
- [ ] SQL-only MCP surface remains unchanged (no non-SQL Fabric tools).
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Requirement IDs `FAB-02`, `FAB-03`, `FAB-04`, `FAB-05`, `SAFE-04` are satisfied by behavior/tests/docs
</success_criteria>

<output>
After completion, create `.planning/phases/03-fabric-sql-endpoint-parity/03-01-SUMMARY.md`
</output>

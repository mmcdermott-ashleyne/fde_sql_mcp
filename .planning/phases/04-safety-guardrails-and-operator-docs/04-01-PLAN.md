---
phase: 04-safety-guardrails-and-operator-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/fde_sql_mcp/tools/databases.py
  - tests/test_query_guardrails.py
  - tests/test_target_routing.py
  - README.md
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - CONF-03
user_setup: []
must_haves:
  truths:
    - "Read-only SQL validation is enforced for both on-prem and Fabric execution paths."
    - "Max rows, max query length, and timeout guardrails behave consistently across on-prem and Fabric targets."
    - "Operators can configure and run dual-environment flows with clear docs for auth precedence and routing examples."
  artifacts:
    - path: "src/fde_sql_mcp/tools/databases.py"
      provides: "Shared guardrail logic used by query and metadata execution paths."
      contains: "_apply_cursor_timeout"
    - path: "tests/test_query_guardrails.py"
      provides: "Cross-target safety regression coverage for SAFE-01/SAFE-02."
      min_lines: 120
    - path: "README.md"
      provides: "Operator setup and usage guide for dual-environment routing."
      contains: "## Operator Guide (On-Prem + Fabric)"
  key_links:
    - from: "src/fde_sql_mcp/tools/databases.py"
      to: "src/fde_sql_mcp/tools/targeting.py"
      via: "active target selection for both query and metadata paths"
      pattern: "get_query_target_impl"
    - from: "README.md"
      to: "src/fde_sql_mcp/tools/targeting.py"
      via: "explicit and natural-language routing examples"
      pattern: "set_query_target"
---

<objective>
Implement Phase 4 safety guardrail parity and operator documentation.

Purpose: codify and verify cross-target read-only safety enforcement and publish clear dual-environment setup/usage guidance without changing the MCP tool surface.
Output: guardrail parity tests, timeout-helper hardening, and expanded README operator docs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-safety-guardrails-and-operator-docs/04-CONTEXT.md
@.planning/phases/04-safety-guardrails-and-operator-docs/04-RESEARCH.md
@src/fde_sql_mcp/tools/databases.py
@src/fde_sql_mcp/tools/targeting.py
@README.md
</context>

<threat_model>
## Threat Model

| threat_id | category | files | disposition | mitigation |
|-----------|----------|-------|-------------|------------|
| T-01 | Read-only validation bypass differs by target context | `src/fde_sql_mcp/tools/databases.py`, `tests/test_query_guardrails.py`, `tests/test_target_routing.py` | mitigate | Add cross-target negative-path tests proving shared validator behavior for on-prem and Fabric routes. |
| T-02 | Timeout/row-limit behavior diverges across query and metadata paths | `src/fde_sql_mcp/tools/databases.py`, `tests/test_query_guardrails.py` | mitigate | Centralize cursor timeout assignment and verify invocation from query + metadata code paths. |
| T-03 | Operator misconfiguration due to incomplete dual-target instructions | `README.md` | mitigate | Add explicit setup checklist, auth precedence notes, and explicit/natural routing examples with expected outcomes. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add cross-target guardrail regression coverage</name>
  <files>tests/test_query_guardrails.py, tests/test_target_routing.py</files>
  <read_first>tests/test_target_routing.py, src/fde_sql_mcp/tools/databases.py, src/fde_sql_mcp/tools/targeting.py</read_first>
  <action>Create `tests/test_query_guardrails.py` with deterministic fake SQL connection fixtures that validate: (a) write keywords are rejected in both on-prem and Fabric target contexts, (b) max query length enforcement is target-agnostic, (c) requested max rows are clamped to configured `sql_max_rows`, and (d) cursor timeout is set to `settings.sql_query_timeout` for both query and metadata execution paths. Add a focused regression in `tests/test_target_routing.py` that confirms Fabric read-only validation triggers before execution for disallowed SQL.</action>
  <verify>.venv\Scripts\python -m pytest tests/test_query_guardrails.py tests/test_target_routing.py -q -k "readonly or timeout or max_rows or query_chars"</verify>
  <acceptance_criteria>
    - `tests/test_query_guardrails.py` exists and includes explicit on-prem and Fabric guardrail assertions.
    - Tests confirm read-only rejection for `INSERT` in both target contexts.
    - Tests confirm query length and max-row guardrails use configured values consistently.
    - Tests confirm query and metadata paths assign cursor timeout from `settings.sql_query_timeout`.
  </acceptance_criteria>
  <done>SAFE-01 and SAFE-02 are regression-protected across both targets.</done>
</task>

<task type="auto">
  <name>Task 2: Harden shared timeout application in execution core</name>
  <files>src/fde_sql_mcp/tools/databases.py, tests/test_query_guardrails.py</files>
  <read_first>src/fde_sql_mcp/tools/databases.py, tests/test_query_guardrails.py, .planning/phases/04-safety-guardrails-and-operator-docs/04-RESEARCH.md</read_first>
  <action>Refactor `src/fde_sql_mcp/tools/databases.py` to introduce `_apply_cursor_timeout(cursor)` and call it from both `_fetch_rows` and `run_readonly_query_impl`. Keep existing semantics: swallow `AttributeError` for pyodbc builds without `cursor.timeout`, preserve current row-limit/query-validation behavior, and keep response schema unchanged.</action>
  <verify>.venv\Scripts\python -m pytest tests/test_query_guardrails.py -q -k "timeout"</verify>
  <acceptance_criteria>
    - `databases.py` contains `_apply_cursor_timeout` and both execution paths call it.
    - Existing behavior for read-only validation, row-limit normalization, and response payload keys remains unchanged.
    - Timeout regression tests pass for both query and metadata paths.
  </acceptance_criteria>
  <done>Timeout guardrails are applied through a single helper with parity across execution paths.</done>
</task>

<task type="auto">
  <name>Task 3: Expand operator docs for dual-environment safety and routing</name>
  <files>README.md</files>
  <read_first>README.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/04-safety-guardrails-and-operator-docs/04-CONTEXT.md</read_first>
  <action>Add an `## Operator Guide (On-Prem + Fabric)` section to `README.md` with: (1) dual-environment setup checklist (allowlists, endpoint map, auth precedence), (2) explicit target-switch examples and natural-language routing examples using current tool signatures, and (3) expected guardrail failures for disallowed SQL, oversized queries, and row-limit clamping. Keep existing tool names/signatures unchanged and document behavior, not new APIs.</action>
  <verify>.venv\Scripts\python -m pytest -q</verify>
  <acceptance_criteria>
    - README includes `## Operator Guide (On-Prem + Fabric)` with setup, auth precedence, and routing examples.
    - README includes concrete explicit and natural-language `set_query_target` examples.
    - README documents read-only and guardrail rejection behavior aligned with SAFE-01/SAFE-02.
  </acceptance_criteria>
  <done>CONF-03 documentation requirements are satisfied for operators using both targets.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.venv\Scripts\python -m pytest tests/test_query_guardrails.py -q` passes.
- [ ] `.venv\Scripts\python -m pytest -q` passes.
- [ ] `run_readonly_query` response schema remains `rows`, `row_count`, `row_limit`, `truncated`, and `target_context`.
- [ ] README documents explicit and natural-language routing examples plus safety guardrail behavior.
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Requirement IDs `SAFE-01`, `SAFE-02`, and `CONF-03` are satisfied by behavior/tests/docs
</success_criteria>

<output>
After completion, create `.planning/phases/04-safety-guardrails-and-operator-docs/04-01-SUMMARY.md`
</output>
